Docker step:
docker build -t fullstack-webapp:dev .
docker run -v ${PWD}:/app -v /app/node_modules -p 4201:4200 --rm fullstack-webapp:dev

Open your browser to http://localhost:4201 and you should see the app.